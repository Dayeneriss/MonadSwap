import { Chain } from "viem";

export const monadTestnet = {
  id: 1337, // À mettre à jour avec l'ID correct une fois qu'il sera public
  name: "Monad Testnet",
  network: "monad-testnet",
  nativeCurrency: {
    decimals: 18,
    name: "MONAD",
    symbol: "MONAD",
  },
  rpcUrls: {
    default: {
      http: ["https://rpc-testnet.monad.xyz"], // À mettre à jour avec l'URL RPC correcte
    },
    public: {
      http: ["https://rpc-testnet.monad.xyz"], // À mettre à jour avec l'URL RPC correcte
    },
  },
  blockExplorers: {
    default: {
      name: "Monad Explorer",
      url: "https://explorer-testnet.monad.xyz", // À mettre à jour avec l'URL de l'explorateur correcte
    },
  },
  testnet: true,
} as const satisfies Chain;
